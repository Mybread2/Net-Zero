rootProject.name = 'netzero'

// 모노레포: backend(Spring Boot)를 Gradle 서브프로젝트로 포함한다.
// 루트에서 IDE로 열면 Gradle 프로젝트로 임포트되고, 태스크는 :backend:<task> 로 실행한다.
// (frontend 는 Gradle 대상이 아니므로 포함하지 않는다.)
include 'backend'
