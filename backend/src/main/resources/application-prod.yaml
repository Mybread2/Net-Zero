spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      # 해커톤: 엔티티 기준으로 테이블 자동 생성/변경(데이터 보존). 스키마 수동 적재 불필요.
      ddl-auto: update
    show-sql: false
